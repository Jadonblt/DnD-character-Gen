pipeline {
    environment {
        DOCKERHUB_CREDENTIALS=credentials'dockerjadon-dockerhub'
    }
    agent any
    stages {
        stage('Build Images') {
            steps {
                sh ''
            }
        }
        stage('Deploy') {
            steps {
                //
            }
        }
    }
}